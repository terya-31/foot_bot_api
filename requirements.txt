# Основные
python-telegram-bot>=22.0,<23.0
requests>=2.32.0

# Для переменных окружения
python-dotenv>=1.0.0

# Для экспорта данных (платная подписка)
pandas>=2.2.0          # для работы с таблицами
openpyxl>=3.1.0        # для экспорта в Excel

# Для кэширования и хранения данных
redis>=5.0.0           # если захотите использовать Redis для кэша (опционально)

# Для логирования (уже есть в стандартной библиотеке)
# logging — встроенный